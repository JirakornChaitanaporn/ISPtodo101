name: Docker Compose CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: kasemsut555
          password: dckr_pat_1bmHQ7rIyE-imcNSVpvWhcxIOg8

      - name: Build and tag Docker images
        run: |
          docker compose -f docker-compose.yml build
      - name: Tag & Push Backend image
        run: |
          docker tag backend:latest kasemsut555/todo-backend:latest
          docker push kasemsut555/todo-backend:latest
      - name: Tag & Push Frontend image
        run: |
          docker tag backend:latest kasemsut555/todo-frontend:latest
          docker push kasemsut555/todo-frontend:latest